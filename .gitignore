# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Env — secrets and local overrides (examples are committed)
.env
.env.local
.env.*.local
apps/web/.env.local
# The Drive sync service-account key JSON — a standing secret, never committed (ADR-0021). Feed it
# to the app base64-encoded as GOOGLE_SERVICE_ACCOUNT_JSON, not as a file.
credentials.json
*-service-account.json

# Test / coverage
coverage/

# Playwright E2E output
playwright-report/
test-results/
.last-run.json
# Per-role sessions the live e2e setup regenerates each run
apps/web/e2e/.auth/

# Editor / OS
.DS_Store
*.log

# design-sync (claude.ai/design import) — the sync inputs now live in temp-folder/
.ds-sync/
ds-bundle/

# Scratch: exported designs, brand PDFs, client docs, anything not part of the build
temp-folder/
