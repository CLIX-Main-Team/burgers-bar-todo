# tasks: map

The Tasks screen (`/tasks` and `/tasks/subjects/:id`, `apps/web/src/features/tasks/`): the
redesign of 2026-09-22 that brought it into the Dashboard's house style.

- design.md: what the page looks like now, level by level, what was removed and why, and the one
  picture it carries (the ticket spike). Read this first.

The departments and subjects model the page is built on is specified in
`docs/superpowers/specs/2026-09-20-departments-and-task-subjects-design.md`.
