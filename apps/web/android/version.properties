# The release identity of the Android app, kept here so a store upload is a one-line
# edit instead of a hunt through build.gradle. Bump versionCode for every single upload:
# Play rejects a bundle whose code is not higher than the last one it accepted, and the
# number can never be reused, even for a build that was discarded.
versionCode=3
versionName=1.0.2
