<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup is off (the Capacitor template ships it on): the signed-in session token
         lives in WebView localStorage, and Android's auto-backup would carry it into the
         cloud and onto whatever device restores it next. Staff re-authenticate instead. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- The status-bar icon and tint for a task notification (#59). Android draws a
             notification icon as a silhouette: every non-transparent pixel is painted, so a
             full-colour launcher icon comes out as a white blob. ic_stat_notify is the bare mark
             cut for exactly that treatment, and the colour below tints it. Both are read by
             Firebase Messaging when the app is in the background, which is the case that matters —
             the foreground one never reaches the status bar. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13 (API 33) and up gate notifications behind a runtime permission; without this
         declaration the prompt can never be shown and every notification is dropped silently.
         Asking for it is the app's job, and it does so only once someone is signed in, so the
         prompt arrives with a reason rather than on first launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
