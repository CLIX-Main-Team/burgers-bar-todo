# apps/web reads ONLY this value (ADR-0010). Copy to apps/web/.env.local (gitignored).
# Keeping it isolated to VITE_API_BASE_URL means no server secret can be VITE-exposed
# into the client bundle.
VITE_API_BASE_URL=http://localhost:3000
